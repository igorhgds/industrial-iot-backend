spring:
  application:
    name: industrial-iot-backend
  threads:
    virtual:
      enabled: true
  datasource:
    url: jdbc:postgresql://localhost:5432/industrial_iiot_db
    username: iiot_user
    password: 135790
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false